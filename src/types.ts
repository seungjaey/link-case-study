export interface CommonConstantItem {
  name: string;
  value: string;
}

export interface AnalysisTargetDataItem {
  targetOriginalName: string;
  targetOriginalUrl: string;
  deviceName: string;
  userAgent: string;
}
